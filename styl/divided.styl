/*

for each in screen sizes
  create media query
    for each divided size 1 to the set max
      set width of element inside .divided-x to below:
        width = 100/x %
*/

// screen sizes
screen-sizes = ( xs sm md lg xl )

// define max dividers
divided-max = 20

// divided-x classes for a defined child column count. All children will take on widths set by the divider to fit inide the container.
[class*='divided']
  text-align: center
  &.equal-height
    display: flex
  &:not(.equal-height) > *
    margin: 0
    padding: 5px
    width: 100%

// setup screen sizes for each divided size
for screen-size in screen-sizes
    
  // media query settings
  min-width-size = screen-size+'-min-width'
  max-width-size = screen-size+'-max-width'
  min-width = 'min-width: '+min-width-size
  max-width = 'max-width: '+max-width-size

  // setup screen sizes for each column size
  @media screen and ({min-width})
    
    for divided in (1..divided-max)
      .{screen-size}-divided-{divided}:not(.equal-height)
        & > *
          float: left
          width: 'calc(%s / %s)' % (100% divided)
